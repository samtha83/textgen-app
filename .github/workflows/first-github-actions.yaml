name: my github workflow
on:
  push:
    branches:
      - main
env:
  project_name: testtextgen
jobs:
  testjob:
    runs-on: ubuntu-latest
    steps:
      - run: echo "checkout the repo"
      - name: checkout repo
        uses: actions/checkout@v4
      - run: printenv
      - run: echo "this is the env ${{env.project_name}}"

